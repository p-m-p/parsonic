{
  "name": "@parsonic/share-button",
  "version": "1.1.0",
  "description": "Native share button web component",
  "main": "index.js",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/p-m-p/parsonic"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --minify --sourcemap --outfile=min.js --format=iife",
    "generate-manifest": "cem analyze --globs 'ShareButton.js'",
    "clean": "rm -f min.*",
    "prepare": "cp ../../LICENSE ./"
  },
  "keywords": [
    "button",
    "component",
    "links",
    "share",
    "social",
    "web"
  ],
  "author": "Phil Parsons",
  "license": "MIT",
  "customElements": "custom-elements.json"
}
